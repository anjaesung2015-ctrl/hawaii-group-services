# 자연어 일정 입력 (NLP Event Parser) — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** schedule-manager에 자연어 입력바를 추가하고, LLM으로 한국어 일정 텍스트를 events 테이블 필드 JSON으로 파싱하여 미리보기 후 확인 시 추가하도록 구현한다.

**Architecture:** 단일 SPA 구조 유지. 신규 엔드포인트 `POST /api/parse-event` 가 `openclaw agent` CLI를 child_process로 호출 → JSON 응답을 클라이언트에 전달 → 미리보기 카드 → [확인] 시 기존 `POST /api/events` 흐름. 기존 `openEventModal` 함수를 prefill 인자 받도록 확장.

**Tech Stack:** Node.js + Express(server.js), better-sqlite3, vanilla JS SPA(public/index.html), `openclaw agent` CLI(이미 EC2에 설치됨).

**작업 환경:** EC2 `/home/ubuntu/.openclaw/workspace/schedule-manager/`. SSH:
```
ssh -i "C:\Users\Asus\Downloads\eunice-key.pem" -o StrictHostKeyChecking=no ubuntu@3.93.96.130 "<command>"
```

**테스트 방법:** 자동 테스트 인프라 없음. 각 Task에서 (a) 구문 검증 (b) curl 실제 호출 (c) 브라우저 수동 검증.

---

## 파일 구조

| 파일 | 책임 | 변경 |
|------|------|------|
| `server.js` | API + LLM 호출 | 수정 (신규 엔드포인트 추가) |
| `server.js.bak-nlpevent-20260506` | 롤백용 백업 | 신규 (Task 1) |
| `public/index.html` | UI/JS | 수정 (입력바 + 미리보기 카드 + openEventModal 확장) |
| `public/index.html.bak-nlpevent-20260506` | 롤백용 백업 | 신규 (Task 2) |
| `schedule.db` | DB | 변경 없음 |

---

## Task 1: LLM 호출 검증 + 백엔드 엔드포인트

### Task 1.1: 백업

- [ ] 1.1 server.js 백업
```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && cp server.js server.js.bak-nlpevent-20260506 && ls -la server.js*"
```
Expected: 두 파일 보임.

### Task 1.2: openclaw agent 호출 검증

핵심 미지수 — agent CLI가 어떻게 응답하는지 확인.

- [ ] 1.2.1 단순 테스트
```
ssh ... "openclaw agent --local --message 'JSON 형식으로만 응답해주세요. {\"test\":\"ok\"}만 출력하세요.' --json --timeout 15 2>&1 | head -50"
```
Expected: stdout에 JSON 또는 에이전트 응답.

- [ ] 1.2.2 결과 분석
응답 형태에 따라 분기:
- **Path A**: `--local` + `--json`이 깔끔한 JSON 응답을 stdout에 줌 → child_process.execFile로 호출
- **Path B**: `--local`이 환경변수 부족 등으로 실패 → `--local` 빼고 게이트웨이 모드로 시도 (세션 시스템 사용)
- **Path C**: 둘 다 안 되면 BLOCKED → 에이전트에게 보고

이 Task에서는 Path A를 우선 가정. 실패 시 implementer가 BLOCKED로 보고 → controller가 Path B 또는 anthropic SDK 직접 호출로 재dispatch.

### Task 1.3: 시스템 프롬프트 작성

`server.js`에 함수 추가 — 백엔드 어디에 넣을지는 1.4에서.

```javascript
function buildParsePrompt(userText, todayStr, weekday) {
  return `당신은 한국어 일정 파서입니다. 사용자 입력을 분석해 JSON으로만 응답하세요. 다른 설명, 코드블록 표시 금지.

오늘 날짜: ${todayStr} (${weekday})
타임존: Asia/Ulaanbaatar

출력 JSON 필드 (모든 필드 포함):
{
  "title": "string (필수)",
  "start_date": "YYYY-MM-DD (필수)",
  "end_date": "YYYY-MM-DD (단일 날짜면 start_date와 같게)",
  "start_time": "HH:MM (24h) 또는 null",
  "end_time": "HH:MM (24h) 또는 null",
  "all_day": true/false,
  "category": "work|personal|health|study|family|meeting 중 하나",
  "location": "string (없으면 빈 문자열)",
  "recurring": "null|weekly|daily|monthly",
  "confidence": 0.0~1.0
}

규칙:
- "오늘/내일/모레/글피"는 오늘 날짜 기준 계산
- "다음주 X요일"은 오늘 이후 가장 가까운 X요일에 7일 더한 날
- "오는 X요일"은 오늘 이후 가장 가까운 X요일
- "오전/오후"는 12시간제로 해석
- 시간 누락 시 all_day=true, start_time/end_time=null
- 모호한 입력은 confidence를 0.5 미만으로
- title은 핵심 명사만 추출 ("미팅", "치과", "운동")

사용자 입력: "${userText}"

JSON만 출력:`;
}
```

### Task 1.4: parse-event 엔드포인트 추가

`server.js` 위치 — 기존 `/api/events` 엔드포인트(line 204) 바로 뒤에 추가.

- [ ] 1.4.1 server.js 상단 require 추가 (이미 있는지 먼저 확인)
```
ssh ... "head -10 /home/ubuntu/.openclaw/workspace/schedule-manager/server.js"
```
`const { execFile } = require('child_process');` 가 있는지 확인. 없으면 5번째 줄 부근에 추가.

- [ ] 1.4.2 buildParsePrompt 함수 + 엔드포인트 추가

`/api/events` POST 엔드포인트 다음에 다음 코드 삽입 (line 209 직후):

```javascript
function buildParsePrompt(userText, todayStr, weekday) {
  return `당신은 한국어 일정 파서입니다. 사용자 입력을 분석해 JSON으로만 응답하세요. 다른 설명, 코드블록 표시 금지.

오늘 날짜: ${todayStr} (${weekday})
타임존: Asia/Ulaanbaatar

출력 JSON 필드:
{
  "title": "string (필수)",
  "start_date": "YYYY-MM-DD (필수)",
  "end_date": "YYYY-MM-DD",
  "start_time": "HH:MM (24h) 또는 null",
  "end_time": "HH:MM (24h) 또는 null",
  "all_day": true/false,
  "category": "work|personal|health|study|family|meeting",
  "location": "string (없으면 \\"\\")",
  "recurring": "null|weekly|daily|monthly",
  "confidence": 0.0~1.0
}

규칙:
- 오늘/내일/모레/글피는 오늘 날짜 기준 계산
- 다음주 X요일은 오늘 이후 가장 가까운 X요일에 7일 더한 날
- 오는 X요일은 오늘 이후 가장 가까운 X요일
- 오전/오후는 12시간제 해석
- 시간 누락 시 all_day=true, start_time/end_time=null
- 모호한 입력은 confidence<0.5
- title은 핵심 명사만 추출

사용자 입력: "${userText}"

JSON만 출력:`;
}

const WEEKDAYS_KO = ['일','월','화','수','목','금','토'];

app.post('/api/parse-event', auth, (req, res) => {
  const text = (req.body?.text || '').toString().trim();
  if (!text) return res.status(400).json({ ok: false, error: 'empty_input' });
  if (text.length > 500) return res.status(400).json({ ok: false, error: 'too_long' });

  const now = new Date(Date.now() + 8*60*60*1000); // UTC+8 Ulaanbaatar
  const todayStr = now.toISOString().slice(0, 10);
  const weekday = WEEKDAYS_KO[now.getUTCDay()];

  const prompt = buildParsePrompt(text, todayStr, weekday);

  execFile('openclaw', ['agent', '--local', '--message', prompt, '--json', '--timeout', '15'], {
    timeout: 18000,
    maxBuffer: 1024 * 1024
  }, (err, stdout, stderr) => {
    if (err) {
      console.error('parse-event execFile error:', err.message, stderr?.slice(0, 500));
      return res.status(502).json({ ok: false, error: 'llm_failed', detail: err.message });
    }
    let parsed = null;
    try {
      // openclaw --json wraps the agent reply; extract first {…} block from anywhere in stdout
      const jsonMatch = stdout.match(/\{[\s\S]*\}/);
      if (!jsonMatch) throw new Error('no_json_in_output');
      const wrapper = JSON.parse(jsonMatch[0]);
      // wrapper may directly be parsed JSON or contain reply text — try both shapes
      const replyText = wrapper.reply || wrapper.message || wrapper.content || jsonMatch[0];
      const innerMatch = (typeof replyText === 'string') ? replyText.match(/\{[\s\S]*\}/) : null;
      parsed = innerMatch ? JSON.parse(innerMatch[0]) : wrapper;
    } catch (e) {
      console.error('parse-event JSON parse error:', e.message, 'stdout:', stdout.slice(0, 500));
      return res.status(502).json({ ok: false, error: 'parse_failed', raw: stdout.slice(0, 500) });
    }
    // validate minimum fields
    if (!parsed.title || !parsed.start_date || !/^\d{4}-\d{2}-\d{2}$/.test(parsed.start_date)) {
      return res.status(422).json({ ok: false, error: 'invalid_fields', parsed });
    }
    if (!parsed.end_date) parsed.end_date = parsed.start_date;
    if (typeof parsed.all_day !== 'boolean') parsed.all_day = !parsed.start_time;
    if (!parsed.category) parsed.category = 'personal';
    if (!parsed.location) parsed.location = '';
    if (parsed.recurring === 'null' || parsed.recurring === undefined) parsed.recurring = null;
    if (typeof parsed.confidence !== 'number') parsed.confidence = 0.7;
    res.json({ ok: true, parsed });
  });
});
```

### Task 1.5: 구문 + 배포

- [ ] 1.5.1 Node syntax check
```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && node -c server.js && echo SYNTAX_OK"
```
Expected: `SYNTAX_OK`.

- [ ] 1.5.2 pm2 restart
```
ssh ... "pm2 restart schedule-manager && sleep 2 && pm2 status schedule-manager | head -3 && pm2 logs schedule-manager --lines 5 --nostream"
```
Expected: `online`, no errors in last 5 log lines.

- [ ] 1.5.3 curl 실제 테스트 (cookie 필요)
```
ssh ... "
TOKEN=\$(curl -s -X POST -H 'content-type: application/json' -d '{\"username\":\"admin\",\"password\":\"admin123\"}' http://localhost:6007/api/login | python3 -c 'import sys,json; print(json.load(sys.stdin)[\"token\"])' 2>/dev/null);
echo \"token: \${TOKEN:0:20}...\";
curl -s -X POST -H 'content-type: application/json' -H \"authorization: Bearer \$TOKEN\" -d '{\"text\":\"내일 3시 미팅\"}' http://localhost:6007/api/parse-event;
"
```
Expected: `{"ok":true,"parsed":{"title":"미팅","start_date":"2026-05-07","start_time":"15:00",...}}`. 만약 admin 비번이 다르면 실제 비번으로 교체. login 응답에 token 키가 없으면 cookie 헤더 사용.

만약 1.5.3가 LLM 응답 형식 문제로 실패 → BLOCKED 보고. controller가 응답 stdout 보고 정규식 또는 prompt 조정.

### Task 1.6: 백업 정리 (Task 1 완료 후)

성공 시 백업 유지(전체 작업 완료 후 정리). 실패 시 즉시 복구:
```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && cp server.js.bak-nlpevent-20260506 server.js && pm2 restart schedule-manager"
```

---

## Task 2: 프론트엔드 - 입력바 위젯

### Task 2.1: index.html 백업

- [ ] 2.1
```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && cp public/index.html public/index.html.bak-nlpevent-20260506"
```

### Task 2.2: renderQuickAdd 함수 추가

`loadCalendarToday` 함수(약 line 204) 앞에 새 함수 추가:

```javascript
function renderQuickAdd(targetSelector) {
  // 동일 위젯을 여러 곳에 삽입 가능. innerHTML 반환.
  return `
    <div class="quick-add-wrap" data-target="${targetSelector}">
      <div class="quick-add-input-row">
        <input type="text" class="quick-add-input" placeholder="✏️ 일정 적기  예: 내일 3시 미팅" 
               onkeydown="if(event.key==='Enter') quickAddSubmit(this)" />
        <button class="btn btn-primary btn-sm quick-add-btn" onclick="quickAddSubmit(this.previousElementSibling)">추가</button>
      </div>
      <div class="quick-add-result"></div>
    </div>
  `;
}

async function quickAddSubmit(inputEl) {
  const text = (inputEl.value || '').trim();
  if (!text) return;
  const wrap = inputEl.closest('.quick-add-wrap');
  const result = wrap.querySelector('.quick-add-result');
  result.innerHTML = '<div style="text-align:center;padding:1rem;color:var(--text2)"><i class="fas fa-spinner fa-spin"></i> 분석 중...</div>';
  try {
    const r = await apiFetch('/api/parse-event', { method:'POST', headers:{'content-type':'application/json'}, body: JSON.stringify({text}) });
    const data = await r.json();
    if (!data.ok) {
      result.innerHTML = `<div class="quick-add-error">😅 이해 못했어요 (${data.error||'알 수 없음'}). <button class="btn btn-sm" onclick="this.closest('.quick-add-wrap').querySelector('.quick-add-result').innerHTML='';openEventModal()">직접 입력</button></div>`;
      return;
    }
    renderPreviewCard(result, data.parsed, text, inputEl);
  } catch(err) {
    console.error('quickAddSubmit error:', err);
    result.innerHTML = `<div class="quick-add-error">😅 네트워크 오류. <button class="btn btn-sm" onclick="this.closest('.quick-add-wrap').querySelector('.quick-add-result').innerHTML=''">다시 시도</button></div>`;
  }
}
```

### Task 2.3: CSS 추가

`<style>` 마지막(`</style>` 직전)에 추가:

```css
.quick-add-wrap{margin-bottom:1rem}
.quick-add-input-row{display:flex;gap:0.5rem;background:var(--card);padding:0.6rem;border-radius:10px;border:1px solid rgba(255,255,255,0.06)}
.quick-add-input{flex:1;background:transparent;border:none;outline:none;color:var(--text);font-size:0.9rem;padding:0.4rem}
.quick-add-input::placeholder{color:var(--text2);opacity:0.7}
.quick-add-btn{white-space:nowrap}
.quick-add-result{margin-top:0.5rem}
.quick-add-error{padding:0.7rem;background:rgba(239,68,68,0.1);border-radius:8px;color:var(--danger);font-size:0.85rem;display:flex;justify-content:space-between;align-items:center;gap:0.5rem}
.preview-card{padding:0.9rem;background:var(--card);border-radius:10px;border-left:3px solid var(--primary)}
.preview-card .pv-row{display:flex;gap:0.5rem;align-items:center;padding:0.25rem 0;font-size:0.85rem}
.preview-card .pv-label{color:var(--text2);min-width:60px;font-size:0.78rem}
.preview-card .pv-warn{color:var(--warning)}
.preview-card .pv-actions{display:flex;gap:0.4rem;margin-top:0.7rem}
```

### Task 2.4: 입력바 노출 — loadDashboard 상단

`loadDashboard` 함수(line ~158) 안에서 첫 `document.getElementById('sec-dashboard').innerHTML = ...` 부분의 첫 줄에 입력바 prefix:

찾기:
```
ssh ... "grep -n \"sec-dashboard').innerHTML\" public/index.html"
```

해당 줄의 innerHTML 템플릿 시작 부분(`\``의 바로 뒤)에 다음 추가:
```javascript
${renderQuickAdd('dashboard')}
```

즉 기존:
```javascript
document.getElementById('sec-dashboard').innerHTML=`
    <h2 style="margin-bottom:0.5rem">📅 오늘: ${today}</h2>
    ...
```

변경:
```javascript
document.getElementById('sec-dashboard').innerHTML=`
    ${renderQuickAdd('dashboard')}
    <h2 style="margin-bottom:0.5rem">📅 오늘: ${today}</h2>
    ...
```

### Task 2.5: 입력바 노출 — loadCalendarToday 상단

`loadCalendarToday` 안의 `target.innerHTML = ` 템플릿 시작에 동일하게 prefix:

기존:
```javascript
target.innerHTML = `
  <h2 style="margin-bottom:0.8rem">📍 오늘 ${today}</h2>
  ...
```

변경:
```javascript
target.innerHTML = `
  ${renderQuickAdd('calendar-today')}
  <h2 style="margin-bottom:0.8rem">📍 오늘 ${today}</h2>
  ...
```

### Task 2.6: 구문 검증

- [ ] 2.6
```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && node -e \"const fs=require('fs'); const html=fs.readFileSync('public/index.html','utf8'); const m=html.match(/<script>([\\s\\S]*?)<\\/script>/); new Function(m[1]); console.log('JS OK');\""
```
Expected: `JS OK`.

브라우저로 https://app.hawaiigroup.co/schedule-manager/ 접속하여 두 위치 모두 입력바 보이는지 확인 (정적 자산이라 pm2 reload 불필요, 강제 새로고침 Ctrl+F5).

---

## Task 3: 미리보기 카드 + 액션

### Task 3.1: renderPreviewCard 함수 추가

Task 2.2의 `quickAddSubmit` 함수 뒤에 추가:

```javascript
function renderPreviewCard(resultEl, parsed, originalText, inputEl) {
  const lowConf = (parsed.confidence || 0) < 0.5;
  const dateRange = parsed.start_date + (parsed.end_date && parsed.end_date !== parsed.start_date ? ' ~ ' + parsed.end_date : '');
  const timeRange = parsed.all_day ? '종일' : ((parsed.start_time || '?') + (parsed.end_time ? ' ~ ' + parsed.end_time : ''));
  const recurringLabel = {weekly:'매주', daily:'매일', monthly:'매월'}[parsed.recurring] || '';
  resultEl.innerHTML = `
    <div class="preview-card">
      ${lowConf ? '<div class="pv-row pv-warn">⚠️ 정확하지 않을 수 있어요. 확인해주세요.</div>' : ''}
      <div class="pv-row"><span class="pv-label">제목</span><strong>${escapeHtml(parsed.title)}</strong></div>
      <div class="pv-row"><span class="pv-label">📅 날짜</span>${dateRange}</div>
      <div class="pv-row"><span class="pv-label">⏰ 시간</span>${timeRange}</div>
      <div class="pv-row"><span class="pv-label">📁 분류</span>${CAT_NAMES[parsed.category] || parsed.category}</div>
      ${parsed.location ? `<div class="pv-row"><span class="pv-label">📍 장소</span>${escapeHtml(parsed.location)}</div>` : ''}
      ${recurringLabel ? `<div class="pv-row"><span class="pv-label">🔁 반복</span>${recurringLabel}</div>` : ''}
      <div class="pv-actions">
        <button class="btn btn-primary btn-sm" onclick='quickAddConfirm(this, ${JSON.stringify(parsed).replace(/'/g, "&#39;")})'>✓ 확인</button>
        <button class="btn btn-sm" onclick='quickAddEdit(this, ${JSON.stringify(parsed).replace(/'/g, "&#39;")})'>✎ 수정</button>
        <button class="btn btn-sm" onclick='quickAddCancel(this, ${JSON.stringify(originalText).replace(/'/g, "&#39;")})'>✗ 취소</button>
      </div>
    </div>
  `;
}

function escapeHtml(s) {
  return String(s ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
}
```

### Task 3.2: 액션 핸들러 — 확인

같은 위치에 추가:

```javascript
async function quickAddConfirm(btn, parsed) {
  btn.disabled = true; btn.textContent = '추가 중...';
  try {
    const r = await apiFetch('/api/events', {
      method: 'POST',
      headers: {'content-type': 'application/json'},
      body: JSON.stringify({
        title: parsed.title,
        start_date: parsed.start_date,
        end_date: parsed.end_date || parsed.start_date,
        start_time: parsed.all_day ? null : parsed.start_time,
        end_time: parsed.all_day ? null : parsed.end_time,
        all_day: parsed.all_day ? 1 : 0,
        category: parsed.category || 'personal',
        location: parsed.location || '',
        recurring: parsed.recurring || null,
        priority: 'normal',
        status: 'planned'
      })
    });
    if (!r.ok) throw new Error('save_failed');
    // 입력바 자리로 복귀 + 현재 화면 새로고침
    const wrap = btn.closest('.quick-add-wrap');
    const input = wrap.querySelector('.quick-add-input');
    const result = wrap.querySelector('.quick-add-result');
    input.value = '';
    result.innerHTML = '<div style="color:var(--success);font-size:0.85rem;text-align:center;padding:0.5rem">✓ 추가됨</div>';
    setTimeout(() => { result.innerHTML = ''; }, 1500);
    // 현재 sec/sub 새로고침
    const active = document.querySelector('.section.active');
    if (active?.id === 'sec-calendar') loadCalendarSub((_curSec.split('/')[1]) || 'today');
    else if (active?.id === 'sec-dashboard') loadDashboard();
  } catch(err) {
    console.error('quickAddConfirm error:', err);
    btn.disabled = false; btn.textContent = '✓ 확인';
    alert('저장 실패. 다시 시도해주세요.');
  }
}
```

### Task 3.3: 액션 핸들러 — 수정 + 취소

```javascript
function quickAddEdit(btn, parsed) {
  // 미리보기 카드 닫고 기존 일정 추가 모달을 prefill 상태로 열기
  const wrap = btn.closest('.quick-add-wrap');
  wrap.querySelector('.quick-add-result').innerHTML = '';
  wrap.querySelector('.quick-add-input').value = '';
  openEventModal(null, null, parsed);
}

function quickAddCancel(btn, originalText) {
  const wrap = btn.closest('.quick-add-wrap');
  wrap.querySelector('.quick-add-result').innerHTML = '';
  // 원래 입력 텍스트 유지하여 재시도 가능
  wrap.querySelector('.quick-add-input').value = originalText;
  wrap.querySelector('.quick-add-input').focus();
}
```

### Task 3.4: 구문 검증

```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && node -e \"const fs=require('fs'); const html=fs.readFileSync('public/index.html','utf8'); const m=html.match(/<script>([\\s\\S]*?)<\\/script>/); new Function(m[1]); console.log('JS OK');\""
```

---

## Task 4: openEventModal에 prefill 인자 추가

기존 시그니처 확인:
```
ssh ... "grep -n 'function openEventModal' /home/ubuntu/.openclaw/workspace/schedule-manager/public/index.html"
```
Expected: `function openEventModal(id, date)` 같은 형태 (line ~1121).

### Task 4.1: 시그니처 확장

`function openEventModal(id, date)` → `function openEventModal(id, date, prefill)` 으로 변경.

함수 본문 안에서 폼 채우는 부분(input value 설정하는 곳)에 prefill 분기 추가. 정확한 위치는 `value=` 또는 `.value =` 패턴 따라가서:

```
ssh ... "sed -n '1121,1180p' public/index.html"
```
로 본문 확인 후, 신규 모드(id=null) 분기에 다음 추가:

```javascript
if (prefill) {
  // 폼이 DOM에 마운트된 직후 prefill 값 채우기
  setTimeout(() => {
    const f = document.querySelector('#event-form');
    if (!f) return;
    if (prefill.title) f.querySelector('[name="title"]').value = prefill.title;
    if (prefill.start_date) f.querySelector('[name="start_date"]').value = prefill.start_date;
    if (prefill.end_date) f.querySelector('[name="end_date"]').value = prefill.end_date;
    if (prefill.start_time && f.querySelector('[name="start_time"]')) f.querySelector('[name="start_time"]').value = prefill.start_time;
    if (prefill.end_time && f.querySelector('[name="end_time"]')) f.querySelector('[name="end_time"]').value = prefill.end_time;
    if (prefill.category && f.querySelector('[name="category"]')) f.querySelector('[name="category"]').value = prefill.category;
    if (prefill.location && f.querySelector('[name="location"]')) f.querySelector('[name="location"]').value = prefill.location;
    if (prefill.all_day && f.querySelector('[name="all_day"]')) f.querySelector('[name="all_day"]').checked = true;
    if (prefill.recurring && f.querySelector('[name="recurring"]')) f.querySelector('[name="recurring"]').value = prefill.recurring;
  }, 50);
}
```

⚠️ form input의 `name` 속성이 실제와 다를 수 있음. 다음 명령으로 확인:
```
ssh ... "grep -nE 'name=\"(title|start_date|end_date|start_time|end_time|category|location|all_day|recurring)\"' public/index.html"
```
실제 name과 다르면 위 코드의 selector 조정.

### Task 4.2: 구문 검증

```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && node -e \"const fs=require('fs'); const html=fs.readFileSync('public/index.html','utf8'); const m=html.match(/<script>([\\s\\S]*?)<\\/script>/); new Function(m[1]); console.log('JS OK');\""
```

---

## Task 5: 배포 + 수동 검증

### Task 5.1: 최종 배포

- [ ] 5.1.1 pm2 reload (server.js 변경 반영)
```
ssh ... "pm2 restart schedule-manager && sleep 2 && pm2 status schedule-manager | head -3"
```

- [ ] 5.1.2 HTTP 200 확인
```
ssh ... "curl -sI http://localhost:6007/ | head -3"
```

### Task 5.2: 브라우저 수동 시나리오

`https://app.hawaiigroup.co/schedule-manager/` 에 admin 로그인 후:

- [ ] 대시보드 상단에 입력바 보임
- [ ] 「일정」 [오늘] 탭 상단에도 동일 입력바 보임
- [ ] "내일 3시 미팅" 입력 후 엔터 → 로딩 → 미리보기 카드 (제목=미팅, 날짜=2026-05-07, 시간=15:00)
- [ ] [✓ 확인] 클릭 → "추가됨" → 입력바 복귀 + 화면 새로고침되며 새 일정 보임
- [ ] "오늘 종일 휴가" 입력 → 미리보기에 종일 표시 → [✓ 확인] OK
- [ ] "ㅁㄴㅇㄹ" 입력 → confidence 낮아 ⚠️ 표시 또는 ok:false
- [ ] "5월 10일 점심 약속" 입력 → [✎ 수정] 클릭 → 기존 일정 추가 모달이 prefill 상태로 열림 → 시간 직접 조정 → 저장 OK
- [ ] [✗ 취소] 클릭 → 입력바로 복귀, 원래 텍스트 유지
- [ ] DB row count 증가 확인:
```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && node -e \"const db=require('better-sqlite3')('schedule.db'); console.log('events:', db.prepare('SELECT COUNT(*) c FROM events').get().c);\""
```

### Task 5.3: 백업 정리

전체 OK 시:
```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && rm -f server.js.bak-nlpevent-20260506 public/index.html.bak-nlpevent-20260506"
```

문제 시 롤백:
```
ssh ... "cd /home/ubuntu/.openclaw/workspace/schedule-manager && cp server.js.bak-nlpevent-20260506 server.js && cp public/index.html.bak-nlpevent-20260506 public/index.html && pm2 restart schedule-manager"
```

---

## 완료 기준

- [ ] `POST /api/parse-event` 엔드포인트 동작 (curl로 검증)
- [ ] 입력바 2곳(대시보드, 「일정」 [오늘]) 노출
- [ ] 자연어 입력 → 미리보기 카드 → [확인]/[수정]/[취소] 모두 동작
- [ ] 5가지 시나리오(내일/오늘 종일/특정날짜/모호/반복) 통과
- [ ] [수정] 시 기존 모달 prefill 동작
- [ ] DB row count 증가
- [ ] 콘솔 에러 0
- [ ] 사용자 OK
